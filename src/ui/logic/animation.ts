import BezierEasing from "bezier-easing"

export const fastEasing = {
  duration: 200,
  easing: BezierEasing(0.64, 0.04, 0.35, 1)
}

export const modalEasing = {
  duration: 200,
  easing: BezierEasing(0.64, 0.04, 0.35, 1)
}
